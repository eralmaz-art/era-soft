# ERA SOFT v0.5.1 Platform Shell — Release Audit

Audit date: 2026-08-08  
Target tag: `v0.5.1-platform-shell`  
Release name: `ERA SOFT Platform Shell`

## Decision

**PASS — ready for stable release after the audited commit is merged to `main`.**

## Repository boundary

- release work is isolated in the `era_soft` custom Frappe application;
- no Frappe or ERPNext core files are present or modified;
- no production data or local site configuration is included;
- the local `frappe-bench`, database runtime, logs, backups, and toolchain remain
  outside this Git repository.

## Sensitive data review

- no tracked `.env` or `.env.*` files;
- no tracked `site_config.json` or `common_site_config.json`;
- no private-key headers, AWS access keys, GitHub tokens, Slack tokens, or
  live/test secret-key signatures detected;
- no tracked database, SQL dump, backup, log, PID, or runtime files;
- the single generic credential-term match is the documented `admin` fallback
  for the isolated local MariaDB launcher, not a production or GitHub secret;
- `.gitignore` explicitly excludes environment files, keys, certificates,
  databases, dumps, backups, logs, PIDs, runtime files, caches, and build output.

## Project metadata

- `README.md` describes the platform boundary, current Construction capability,
  operations documentation, release notes, and license;
- `LICENSE` declares ERA SOFT proprietary to ERA Group and preserves all
  third-party license boundaries;
- application and hook versions agree at `0.5.1`;
- the largest tracked artifact is the 48 KB macOS application icon; no large
  local or generated artifacts are included.

## Verification

- 12 repository contract tests passed;
- 8 live Frappe integration tests passed;
- Python package compilation passed;
- workspace and sidebar JSON validation passed;
- Frappe migration completed successfully;
- ERA SOFT asset production build completed successfully;
- visual verification confirmed three application cards, four platform-service
  cards, two management cards, a product roadmap, and a working Construction
  navigation link;
- responsive verification confirmed a single-column narrow layout and the
  intended three/two-column desktop layout.

## Product truthfulness

The release does not claim that every displayed application is implemented.
Lifecycle labels distinguish active, in-development, planned, platform-service,
concept, and foundation states. The roadmap also records that Construction MVP
Part 1 already works and identifies the next procurement slice.

## Publication checklist

- [x] release scope reviewed;
- [x] sensitive-data scan passed;
- [x] README, LICENSE, and `.gitignore` reviewed;
- [x] tests, migration, assets, and UI verified;
- [ ] audited commit merged to `develop` and `main`;
- [ ] tag pushed and GitHub release published;
- [ ] final Git status confirmed clean.
